---@meta
---@class web_server
---@field is_enabled boolean True if the web server is enabled
---@field http_port integer The port the HTTP web server is listening on, 0 if disabled
---@field https_port integer The port the HTTPS web server is listening on, 0 if disabled
web_server = {}

return web_server